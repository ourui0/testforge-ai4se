# TestForge 设计规约

> 状态：设计、计划与冷启动修订均已批准
> 日期：2026-08-04  
> 项目类型：AI4SE A 类 Coding Agent Harness

## 1. 问题陈述

### 1.1 要解决的问题

现有 LLM 可以快速生成 pytest 测试，但“可以运行”不等于“有缺陷发现能力”。常见问题包括只覆盖正常路径、断言薄弱、错误使用 mock、为了通过而修改生产代码，以及生成依赖网络或本地环境的不稳定测试。开发者仍需反复检查覆盖率、分析存活变异、修正测试并控制修改风险。

TestForge 是一个面向 Python + pytest 项目的测试生成 Coding Agent Harness。用户指定本地仓库和目标 Python 文件或模块后，TestForge 在隔离环境中建立质量基线，调用 LLM 生成候选单元测试，通过 pytest、分支覆盖率和变异测试构造客观反馈，并在有限轮次内迭代改进。最终测试以 diff 形式交由用户批准后写回。

### 1.2 目标用户

- 需要为已有 Python 模块补充高价值单元测试的个人开发者；
- 希望评估 AI 生成测试质量的课程项目或研究人员；
- 需要在严格边界内使用 LLM 辅助测试工作的代码维护者。

### 1.3 为什么值得做

TestForge 将“生成测试”从一次性文本生成转化为可观测、可暂停、可恢复、可验证的工程闭环。项目重点不在测试数量，而在确定性反馈如何驱动 LLM 改进测试，以及 Harness 如何保证生成代码不会越权作用于真实仓库。

## 2. 目标与非目标

### 2.1 目标

1. 为用户指定的单个 Python 文件或模块生成 pytest 单元测试。
2. 通过 pytest、coverage.py 和 mutmut 建立基线并评价候选测试。
3. 用结构化反馈驱动状态机中的多轮测试改进。
4. 默认禁止修改生产代码；必要重构必须逐补丁人工审批。
5. 在临时 Docker 沙箱中运行生成代码，限制网络与资源。
6. 使用 mock LLM 离线、确定性地验证全部核心机制。
7. 通过 CLI 和本地 WebUI运行真实任务，通过公网 WebUI演示 mock 场景。

### 2.2 第一版不做

- 不支持 Python 以外的语言；
- 不生成集成测试、端到端测试、性能测试或真实外部服务测试；
- 不并行处理多个模块或多个候选补丁；
- 不向 LLM 暴露任意 Shell、Docker或网络工具；
- 不在线上执行用户上传或克隆的代码；
- 不使用 RAG 或向量数据库；
- 不自动写回测试，不自动修改生产代码；
- 不实现多用户权限、团队协作或微服务架构。

## 3. 用户故事

1. 作为 Python 开发者，我希望指定一个模块并获取当前测试、分支覆盖率与变异测试基线，以便知道测试缺口是否真实存在。
2. 作为 Python 开发者，我希望 TestForge 根据未覆盖分支与存活变异生成候选测试，以便得到比“只会通过”的测试更有价值的结果。
3. 作为代码所有者，我希望生产代码默认只读，以便 LLM 不能通过修改实现来让测试虚假通过。
4. 作为代码所有者，我希望在任何生产代码重构前看到原因、风险与具体 diff，并能批准或拒绝，以便保留最终控制权。
5. 作为代码所有者，我希望候选测试先在隔离副本中验证，只有达到质量门槛且经我批准后才写回，以免污染真实仓库。
6. 作为用户，我希望任务在等待审批或进程重启后可以恢复，以免丢失已经完成的反馈循环。
7. 作为用户，我希望查看每轮测试结果、覆盖率变化、变异得分和停止原因，以便判断 Agent 是否真正改进了测试。
8. 作为维护者，我希望使用脚本化 mock LLM 离线复现关键路径，以便在没有网络和真实 API Key 时测试 Harness。
9. 作为首次使用者，我希望安全录入、查看配置状态、更新和清除 OpenAI API Key，以免凭据泄露到源码或日志。
10. 作为评审者，我希望通过公网演示在无真实凭据和无不可信代码执行的情况下观察护栏、反馈修正和审批流程。

这些故事彼此独立、可协商、有用户价值、可估算、范围较小，并具有明确测试方式，符合 INVEST 原则。

## 4. 功能规约

### 4.1 项目初始化与目标验证

- **输入**：本地仓库路径、目标 Python 文件或模块、测试目录及可选配置覆盖。
- **行为**：规范化路径；检查目标位于仓库内；发现 pytest 配置；验证 Docker 可用；生成不含凭据的项目配置。
- **依赖环境**：用户明确确认仓库可信后，初始化阶段使用固定的多阶段 Dockerfile 构建本地项目运行镜像。构建阶段可访问包索引并执行项目构建后端；最终运行镜像只包含虚拟环境，不包含项目源码，且不会被 TestForge 自动推送。
- **输出**：项目记录、有效配置摘要或结构化输入错误。
- **边界**：一次任务只允许一个目标模块；符号链接解析后仍须位于仓库根目录内。
- **错误处理**：路径越界、模块不存在、配置非法或 Docker 不可用时不创建可运行任务。

### 4.2 基线建立

- **输入**：目标模块、现有测试及任务质量配置。
- **行为**：在临时沙箱中依次运行 pytest、coverage.py 和 mutmut；记录源码与测试哈希。
- **输出**：pytest 统计、语句/分支覆盖率、变异统计和基线快照。
- **边界**：基线工具必须在预算内完成；不得写入原仓库。
- **错误处理**：现有测试失败、工具不可运行或结果无法解析时任务停止在可诊断状态，不进入生成阶段。

### 4.3 测试生成

- **输入**：目标源码摘要、相关现有测试、项目约定、质量基线及上一轮结构化反馈。
- **行为**：通过供应商无关的 LLM 接口请求结构化 `TestProposal`。
- **输出**：候选测试补丁或生产代码重构申请。
- **边界**：只能提议测试文件变更；不能返回 Shell 命令或直接执行工具。
- **错误处理**：响应 schema 无效时形成有限重试反馈；连续失败后以 `LLMError` 停止。

### 4.4 候选测试验证

- **输入**：候选测试补丁及基线沙箱。
- **行为**：校验路径与补丁大小，在沙箱副本中应用补丁，运行 pytest、coverage.py 和 mutmut。
- **输出**：`TestResult`、`CoverageDelta`、`MutationDelta` 与脱敏诊断。
- **边界**：禁止网络、外部数据库和真实外部服务；临时文件必须使用隔离目录。
- **错误处理**：语法、导入、断言、fixture/mock、超时、不稳定、覆盖率无提升和存活变异分别分类。

### 4.5 反馈与迭代

- **输入**：工具结果、历史尝试摘要和剩余预算。
- **行为**：由确定性解析器生成 `FeedbackPacket`，质量门槛决定成功、重试、审批或停止。
- **输出**：下一轮约束、状态转换和指标时间线。
- **边界**：连续两轮指标不变视为停滞；达到轮次、时间、LLM 调用或变异数量预算必须停止。
- **错误处理**：可重试工具错误有限重试；策略违规和过期工作区不自动重试。

### 4.6 重构审批

- **输入**：重构原因、生产代码补丁、风险和替代方案。
- **行为**：进入等待审批状态；批准绑定具体补丁哈希；批准后只在沙箱中应用并重新建立基线。
- **输出**：批准、拒绝或过期决定。
- **边界**：只允许与可测试性直接相关的最小修改；补丁变化后原批准失效。
- **错误处理**：重构导致原有测试失败时拒绝候选并恢复先前沙箱快照。

### 4.7 测试写回审批

- **输入**：已达标的测试 diff、指标变化及相关文件哈希。
- **行为**：展示 diff；批准后复核真实仓库哈希并原子写回测试文件。
- **输出**：写回结果或冲突报告。
- **边界**：不得自动批准；不得覆盖用户在任务期间产生的新修改。
- **错误处理**：哈希不一致时进入 `STALE`，要求重新建立基线和验证。

### 4.8 CLI 与 WebUI

- CLI 提供初始化、凭据管理、运行、状态、审批、应用、历史和本地服务命令。
- 本地 WebUI提供新建任务、状态时间线、指标、diff、审批、项目配置、凭据状态和记忆管理。
- 公网 WebUI只运行内置示例和 mock LLM，不接受源码、仓库地址或用户 API Key。

## 5. 领域与机制设计

### 5.1 领域动作与工具

LLM 只能请求以下领域工具：读取目标源码、读取相关测试、写入候选测试、运行 pytest、测量覆盖率、运行变异测试、提交重构审批、应用已批准重构和导出最终补丁。`DomainToolDispatcher` 对未知工具、非法参数和未经批准的动作进行确定性拒绝，不提供任意 Shell。

### 5.2 客观反馈信号

- pytest 的通过、失败、跳过、超时与 traceback；
- coverage.py 的语句覆盖率、分支覆盖率、未覆盖行与分支；
- mutmut 的变异总数、已杀死、存活、超时和不可运行数量；
- 现有测试是否回归、候选测试是否稳定；
- 候选补丁是否违反路径、文件类型、大小或审批规则。

这些信号由代码解析、分类并回灌，不依赖 LLM 自我评价。

### 5.3 危险动作

- 修改生产代码；
- 将候选测试写回真实仓库；
- 访问仓库外路径或通过符号链接逃逸；
- 删除文件、访问凭据、Git 元数据或 Docker Socket；
- 运行任意 Shell、网络请求或超出资源预算的动作。

治理代码在执行前拦截危险动作；生产代码重构和最终写回必须人工审批。

### 5.4 记忆需求

跨会话保存测试目录、pytest 配置、历史基线、已尝试策略、失败原因和审批决定。不保存完整源码、完整 prompt、API Key 或未经脱敏的日志。记忆通过确定性标签和规则按需选择，不使用框架内置 memory 或 RAG。

### 5.5 主要贡献

项目以**反馈闭环**为主要贡献：将覆盖率与变异测试结果转换为结构化反馈，驱动单候选、多轮、有限预算的测试改进。治理作为必要支撑：提供沙箱、文件围栏、审批状态机和补丁哈希绑定。

### 5.6 自研边界

必须自行实现 Agent 状态机、上下文组织、LLM 抽象、结构化动作解析、工具分发、质量门槛、反馈分类、记忆读写、治理策略、审批与停止条件。允许使用 OpenAI 单次模型 API、HTTP/SDK、pytest、coverage.py、mutmut、Docker SDK、数据库和解析库；不得使用现成 Agent Runner 或框架主循环。

## 6. 状态机与数据流

### 6.1 状态

`CREATED → VALIDATING_INPUT → PREPARING_SANDBOX → BASELINING → GENERATING → TESTING → MEASURING_COVERAGE → MUTATION_TESTING → EVALUATING`

`EVALUATING` 后：

- 达标：`AWAITING_APPLY_APPROVAL → APPLYING_PATCH → COMPLETED`；
- 未达标且有预算：回到 `GENERATING`；
- 需要重构：`AWAITING_REFACTOR_APPROVAL`，批准后重新建立基线；
- 预算耗尽、取消或不可恢复错误：`STOPPED`、`FAILED` 或 `CANCELLED`。

若基线已经没有有效存活变异且分支覆盖率不低于 90%，任务直接进入 `NO_ACTION_NEEDED`，不得用无指标提升的候选测试制造虚假成功。

工作区哈希或配置变化时进入 `STALE`。

### 6.2 单轮数据流

1. 按目标读取源码、相关测试和结构化记忆；
2. 构造最小上下文并调用 LLM；
3. 解析结构化测试补丁或重构申请；
4. 由治理策略校验动作；
5. 在沙箱副本中应用候选补丁；
6. 运行 pytest、coverage.py 与 mutmut；
7. 解析为结构化结果；
8. 质量门槛判断；
9. 未达标则构造下一轮反馈，达标则等待写回审批。

## 7. 质量门槛

候选测试首先必须满足：全部原有和新增测试通过；不增加跳过、预期失败或不稳定测试；覆盖率和变异得分不得下降；测试不访问网络或真实外部服务。

默认平衡提升门槛为：

- 存在有效存活变异时，至少新增杀死 1 个变异，且变异得分提高至少 5 个百分点；
- 目标代码产生零个有效变异，或 mutmut 明确报告目标不支持变异时，分支覆盖率提高至少 5 个百分点或从低于 90% 提升到 90% 及以上。

门槛可以通过项目配置覆盖，但配置必须通过 schema 校验并固化在任务快照中。

工具崩溃、超时或结果解析失败不属于“无法产生有效变异”，不得静默退化到覆盖率门槛。

## 8. 系统架构

### 8.1 组件

- `AgentEngine`：状态机与任务推进；
- `LLMClient`：供应商无关接口，包含 `MockLLMClient` 与 `OpenAIClient`；
- `DomainToolDispatcher`：领域工具注册、参数校验和分发；
- `SandboxRunner`：临时工作区、Docker 生命周期和资源限制；
- `ProjectImageBuilder`：根据项目依赖文件指纹构建本地、不可自动推送的运行镜像；
- `FeedbackEngine`：结果解析、失败分类和反馈生成；
- `QualityGate`：相对质量门槛判定；
- `GovernancePolicy`：路径、补丁、预算和审批规则；
- `ProjectMemory`：结构化项目记忆；
- `TaskRepository`：任务、尝试、指标、审批和审计事件持久化；
- `ApplicationService`：供 CLI 与 WebUI 共用的用例接口。

### 8.2 外部依赖

OpenAI 模型服务、Docker Engine、pytest、coverage.py、mutmut、操作系统钥匙串，以及可选的容器部署平台。mock 模式不依赖网络、真实模型或真实凭据。

## 9. 数据模型

- `Project`：仓库标识、规范化路径、测试目录、pytest/Docker 配置和版本；
- `Task`：目标模块、状态、质量门槛、预算快照、基线哈希和结果；
- `Attempt`：轮次、提案摘要、补丁哈希、工具结果、指标变化和失败分类；
- `MetricSnapshot`：pytest、覆盖率和变异统计；
- `ApprovalRequest`：审批类型、补丁哈希、风险说明、状态和决定信息；
- `MemoryEntry`：类型、内容摘要、来源任务、版本和过期时间；
- `AuditEvent`：状态转换、工具类别、治理决定和脱敏错误摘要。

状态转换与审计事件在同一事务中提交。CLI 默认使用 SQLite；服务端复用仓储接口，第一版不同时实现 PostgreSQL。

## 10. 非功能性需求

### 10.1 安全与凭据威胁模型

主要威胁包括：凭据进入源码或日志、LLM 越权修改、路径穿越、符号链接逃逸、生成代码访问宿主机或网络、资源耗尽、审批后补丁被替换、并发修改导致覆盖用户工作。

对策包括：操作系统钥匙串、隐藏录入和统一脱敏；`.env` 仅作显式开发回退并默认忽略；领域工具白名单；路径规范化和 realpath 校验；Docker 非 root、禁网、资源限制且不挂载敏感资源；审批绑定补丁哈希；写回前复核仓库哈希；任务预算和可取消机制。项目依赖镜像仅在用户确认信任仓库后构建，构建上下文排除 `.env`、`.git`、密钥文件和用户主目录；镜像标签绑定依赖指纹，依赖变化后必须重建。构建阶段可能执行项目构建后端，因此不接受陌生仓库。

### 10.2 性能

- 默认最多进行 5 轮测试生成、调用 LLM 6 次，并使用 45 分钟主动执行时间；等待人工审批的时间不计入主动执行时间；
- pytest 和 coverage.py 的单次调用默认超时 60 秒；
- 每轮变异测试默认最多运行 10 分钟，单任务最多评估 100 个变异；
- WebUI 状态读取不触发昂贵工具执行；
- 用户可以在项目配置中降低或提高预算，但必须通过正数、最大值和交叉字段约束校验；任务开始后使用不可变预算快照。

### 10.3 可用性与可靠性

- CLI 错误必须给出可执行的修复提示；
- TestForge 本身支持 Python 3.11 及以上版本；开发与测试必须在项目内 `.venv` 中运行，不能依赖调用者碰巧激活的全局解释器；
- Windows 与 POSIX 的文档命令必须明确解释器选择方式。文档中的仓库相对路径和 `.gitignore` 统一使用 `/`，运行时路径运算统一使用 `pathlib.Path`；
- pytest 临时文件固定写入项目内且已忽略的 `.pytest_tmp/`，避免受限 Windows 环境无法访问用户级临时目录；每次只顺序运行一个测试任务，不共享该目录；
- 等待审批、进程重启或 WebUI刷新后任务可恢复；
- 状态动作保持幂等，避免重复应用补丁；
- 哈希不一致时拒绝继续，而不是猜测合并。

### 10.4 可观测性

使用任务关联 ID 和结构化日志，记录状态转换、工具类别、耗时、预算与指标摘要。不得记录完整源码、完整 prompt、API Key 或未经脱敏的 traceback。WebUI 展示状态时间线、每轮指标、耗时、预算及停止原因。

## 11. 错误处理

统一领域错误包括 `InputError`、`ConfigurationError`、`CredentialError`、`LLMError`、`SandboxError`、`ToolExecutionError`、`PolicyViolation` 和 `StaleWorkspaceError`。

限流、临时网络错误和部分工具瞬态错误可以有限重试；策略违规、非法配置、过期工作区和审批缺失不自动重试。每种错误明确映射为重试、暂停、失败或人工处理。

## 12. 凭据、分发与部署

### 12.1 凭据

默认通过 Python keyring 使用操作系统钥匙串。首次运行隐藏录入；支持 `set`、`status`、`clear`，状态查询不回显明文。`.env` 仅在显式开发模式下加载，并在 README 中说明明文和进程环境风险。公网演示不使用真实 LLM 凭据。

### 12.2 分发

- CLI 通过 PyPI 包分发；
- 本地与公网 WebUI 提供 Docker 镜像；
- README 说明安装、运行、钥匙串配置、Docker Secret/部署平台 Secret、平台依赖和已知限制；
- CI 运行测试、构建 Python 包、构建容器并执行 mock 演示冒烟测试。

### 12.3 部署边界

本地 CLI/WebUI 可处理用户信任的真实仓库。公网 WebUI只运行内置 fixture 和 mock LLM，不接受上传、仓库 URL、外部网络或用户 API Key，并显著标注演示模式。

真实任务初始化时会为可信项目构建本地运行镜像。依赖获取只发生在这一显式初始化步骤；候选测试、覆盖率和变异测试运行阶段始终使用已构建镜像并关闭网络。TestForge 不自动推送包含项目依赖的本地镜像。

## 13. 技术选型与理由

- Python 3.11+：`StrEnum` 等所需标准库能力从 3.11 起可用，并与冷启动开发环境一致；发布用 Docker 镜像固定使用 Python 3.12，但不把 3.12 设为本地开发硬门槛；
- Typer：类型友好的 CLI；
- FastAPI：复用 Pydantic 模型并提供本地/公网 Web API；
- Pydantic：动作、配置和 LLM 结构化响应校验；
- SQLAlchemy + SQLite：事务化状态与轻量本地部署；
- FastAPI 模板 + HTMX：满足轻量 WebUI，避免独立前端构建链；
- Open Design 原则：保持界面一致性、可访问性与明确状态反馈；
- Python keyring：跨平台访问操作系统凭据存储；
- Docker：隔离生成代码执行；
- pytest、coverage.py、mutmut：提供确定性测试反馈。

## 14. 测试与机制演示

### 14.1 测试层级

1. 纯单元测试：状态转换、质量门槛、反馈分类、配置、路径围栏和审批绑定；
2. mock-LLM 循环测试：脚本化响应复现失败、反馈、修正与成功；
3. 工具集成测试：在固定 Python fixture 中运行 pytest、coverage.py、mutmut 和 Docker；
4. 端到端演示测试：通过 CLI或演示 WebUI完成完整任务。

### 14.2 必须提交的机制演示

1. 治理护栏确定性拦截生产代码未授权修改或目录越界；
2. 注入一次薄弱测试，反馈包指出存活变异，mock LLM 下一轮增加有效断言并改变动作；
3. 反馈闭环达到平衡门槛并进入写回审批，而不是自动修改真实仓库。

## 15. 验收标准

1. 能初始化 Python + pytest 项目并验证单个目标模块；
2. 能在 Docker 沙箱中建立 pytest、分支覆盖率和变异基线；
3. mock LLM 下可离线、确定性运行完整状态机；
4. 至少一次失败反馈会使下一轮候选动作发生可断言的变化；
5. 质量门槛按基线与候选指标确定性判定；
6. 未授权源码写入、目录越界、未知工具和超预算动作被拒绝；
7. 重构审批与写回审批可暂停、持久化和恢复；
8. 最终测试补丁只有在达标、批准和哈希复核后写回；
9. API Key 可安全录入、查询状态、更新和清除，且不会进入日志或 Git；
10. CLI 和本地 WebUI可执行真实任务；
11. 公网 WebUI可用内置示例和 mock LLM 演示三项核心机制；
12. 一键测试通过，CI 构建 PyPI 与 Docker 产物，最后一次 CI 状态为通过。

## 16. 风险与已定处理原则

- **mutmut 性能较慢**：通过时间、变异数量和目标模块范围预算控制；
- **不同项目的 pytest 布局差异**：初始化阶段显式检测并允许项目配置覆盖；
- **LLM 生成脆弱或无意义断言**：使用回归、分支覆盖和变异测试共同评价；
- **Docker 在部分环境不可用**：真实任务明确要求 Docker，公网 mock 演示不依赖运行不可信代码；
- **项目依赖安装可能执行构建代码**：只允许用户确认信任的本地仓库，构建上下文排除敏感文件，最终镜像不复制源码且不自动推送；依赖指纹变化后旧镜像不得复用；
- **符号链接与竞态导致越界**：解析真实路径，操作前后复核哈希，并对写回采用原子替换；
- **重构审批扩大范围**：限制为可测试性最小修改，补丁逐次审批，变更即失效；
- **CI 平台要求存在文档冲突**：实现时保持测试命令平台无关，并在教师确认前准备兼容 GitHub Actions 与 `.gitlab-ci.yml` 的计划；
- **线上 WebUI要求与不可信代码执行冲突**：公网只提供显式 mock 演示，真实仓库执行限定在本地。

## 17. 设计决策摘要

- 采用状态机驱动的单候选顺序反馈循环；
- 用户指定单个 Python 文件或模块；
- 纯 pytest 单元测试；
- 反馈闭环为主贡献，治理为辅；
- 项目级结构化记忆，不使用 RAG；
- 领域专用工具，不提供任意 Shell；
- Docker 沙箱、默认禁网、资源受限；
- 生产代码默认禁止修改，重构逐补丁审批；
- 达标测试展示 diff，批准后写回；
- CLI 核心 + 本地 WebUI，公网只运行 mock 演示；
- PyPI 包 + Docker 镜像分发。
